# AI Lab Research 2026-08-24
23 скаутів у паралель. Унікальних лідів: 96 (дублікатів відкинуто: 0). Пройшли Filter: 3. Відхилено: 93.

# Кандидати — потребують твого kill-test сьогодні ввечері

## Google Maps JavaScript API removals (DirectionsService, DistanceMatrixService, Heatmap, Drawing library)

**Проблема:** Google is removing several widely-used Maps JavaScript API classes — the legacy Directions Service, Distance Matrix Service, Heatmap Layer, and Drawing Library — from the Maps JavaScript API by roughly May 2026. Sites and apps that call these classes directly will throw runtime errors after removal, forcing every affected developer to migrate to the newer Routes API / replacement widgets on a fixed timeline, not at their own pace.
- Джерело: https://developers.google.com/maps/deprecations (unknown (page is continuously updated; retrieved August 2026)) — «"The Heatmap Layer and Drawing Library were deprecated in August and May 2025 respectively, with removal scheduled for May 2026. DirectionsService and DistanceMatrixService were deprecated on February 25, 2026, also with a May 2026 removal target."»
- Незалежних джерел: 2
  - url: https://mapatlas.eu/blog/google-maps-api-deprecations-migration | date: 2026 | quote: "After May 2026, the deprecated classes will be removed from the Maps JavaScript API, and any code that references them will throw runtime errors."
  - url: https://stadiamaps.com/blog/google-maps-heatmap-deprecated-maplibre-migration/ | date: 2025/2026 | quote: Discusses the Heatmap API deprecation and migration to MapLibre-based alternatives.
- Існуючі рішення: name: none found (only vendor migration guides/blog posts, no dedicated migration tool) | url: | monetization: unknown
- Дедлайн-подія: Removal of DirectionsService, DistanceMatrixService, Heatmap Layer, and Drawing Library from the Maps JavaScript API (~May 2026) https://developers.google.com/maps/deprecations
- Чому ніша може бути порожня (гіпотеза): Guess — the affected surface area (a handful of well-documented JS classes) may be small enough that Google's own migration guide and manual code search/replace is "good enough," leaving little room for a paid tool beyond a linter/codemod.

**Filter:**
1. Є вже продукт? no 
2. Є дедлайн? yes ~May 2026 removal of DirectionsService, DistanceMatrixService, Heatmap Layer, Drawing Library
3. Kill-test за вечір без коду? yes

**Kill-test (зроби це сьогодні ввечері):** Use GitHub code search for `"google.maps.DirectionsService("` and `"google.maps.DistanceMatrixService("` filtered to files pushed/updated in the last 6 months, and count distinct public repos returned. Separately search Stack Overflow for questions tagged google-maps-api-3 containing "DirectionsService deprecated" or "migrate Routes API" posted in 2026, and count them.
**Ідея мертва, якщо:** dead if fewer than 30 distinct recently-updated public repos still call these classes AND fewer than 10 related Stack Overflow questions exist from 2026

## HubSpot Pipelines API v1 sunset forcing CRM integration rewrites

**Проблема:** HubSpot is sunsetting the Pipelines API v1 (the official changelog says December 4, 2026; at least one third-party site cites November 2, 2026), after which calls to v1 pipeline endpoints stop working. Agencies and SaaS vendors that built HubSpot integrations years ago on v1 must find and rewrite every pipeline-related call to v3 before the cutoff, and the conflicting dates circulating online make it harder for smaller dev shops to know their real deadline.
- Джерело: https://developers.hubspot.com/changelog/pipelines-api-v1-sunset (unknown (HubSpot changelog entry)) — «"HubSpot will sunset the Pipelines API V1 on December 4, 2026... you'll need to migrate to the 2026-03 or newer version before the deadline to avoid disruption."»
- Незалежних джерел: 2
  - url: https://www.sidekickstrategies.com/hubspot-updates/pipelines-api-v1-deprecation-migrate-v3-hubspot | date: 2026 | quote: cites the deprecation as "Migrate to V3 by Nov 2026," a different date than HubSpot's own changelog.
  - url: https://developers.hubspot.com/docs/api-reference/deprecated-apis | date: unknown | quote: HubSpot's canonical list of sunsetted and deprecated APIs, including pipeline-related endpoints.
- Існуючі рішення: name: none found (no third-party migration/scanning tool specific to HubSpot API version sunsets) | url: | monetization: unknown
- Дедлайн-подія: Pipelines API v1 endpoints stop working (per HubSpot's own changelog) (December 4, 2026) https://developers.hubspot.com/changelog/pipelines-api-v1-sunset
- Чому ніша може бути порожня (гіпотеза): Guess — HubSpot's ecosystem is large but each individual API sunset is narrow (one endpoint family), so the addressable set of affected developers per sunset event may be too small to support a standalone product versus a one-off consulting fix.

**Filter:**
1. Є вже продукт? no 
2. Є дедлайн? yes December 4, 2026 (per HubSpot's own changelog; one third-party source cites November 2, 2026)
3. Kill-test за вечір без коду? yes

**Kill-test (зроби це сьогодні ввечері):** Search the HubSpot Developer Community forum (community.hubspot.com) for threads mentioning "Pipelines API v1" or "pipelines sunset" posted in the last 3 months and count them. Also check the issue trackers of popular HubSpot API client libraries (e.g. hubspot-api-nodejs, hapipy) for open issues referencing v1 pipeline endpoints and count those.
**Ідея мертва, якщо:** dead if fewer than 8 combined community threads/GitHub issues mention this in the last 3 months

## Exchange Online SMTP AUTH (Basic Auth) being disabled by default for legacy scanners/printers/apps

**Проблема:** Microsoft is disabling SMTP AUTH Basic Authentication by default for existing Exchange Online tenants at the end of December 2026 (admins can still manually re-enable it until a later final removal date). This breaks a long tail of legacy line-of-business apps, office scanners/multifunction printers, and scripts that relay mail via SMTP AUTH with a username/password, which now must be reconfigured to OAuth2/Modern Auth — something many small IT shops and non-technical office admins don't know how to do for embedded/appliance devices that don't support OAuth at all.
- Джерело: https://techcommunity.microsoft.com/blog/exchange/updated-exchange-online-smtp-auth-basic-authentication-deprecation-timeline/4489835 (unknown (Microsoft Tech Community post)) — «"Currently through December 2026, SMTP AUTH Basic Authentication behavior remains unchanged, but at the end of December 2026, SMTP AUTH Basic Authentication will be disabled by default for existing tenants, though administrators will still be able to enable it if needed."»
- Незалежних джерел: 2
  - url: https://learn.microsoft.com/en-us/exchange/clients-and-mobile-in-exchange-online/deprecation-of-basic-authentication-exchange-online | date: unknown | quote: Microsoft's official deprecation-of-Basic-auth documentation page.
  - url: https://cyberpress.org/exchange-online-deprecates-smtp-auth/ | date: 2026 | quote: "Exchange Online SMTP AUTH Deprecation Looms – Tenants Urged To Migrate Now."
- Існуючі рішення: name: none found specific to legacy-device SMTP relay migration (Microsoft's own OAuth migration guide and MSAL/oidc-client libraries exist for app developers, but not for hardware appliances) | url: | monetization: unknown
- Дедлайн-подія: SMTP AUTH Basic Authentication disabled by default for existing Exchange Online tenants (end of December 2026) https://techcommunity.microsoft.com/blog/exchange/updated-exchange-online-smtp-auth-basic-authentication-deprecation-timeline/4489835
- Чому ніша може бути порожня (гіпотеза): Guess — the common workaround (buy a third-party SMTP relay/gateway service, e.g. a paid relay that still accepts Basic Auth internally and forwards via OAuth) may already be well covered by existing transactional-email/relay providers, reducing the appeal of a purpose-built new product.

**Filter:**
1. Є вже продукт? no 
2. Є дедлайн? yes end of December 2026, SMTP AUTH Basic Authentication disabled by default for existing tenants
3. Kill-test за вечір без коду? yes

**Kill-test (зроби це сьогодні ввечері):** Search r/sysadmin, r/msp, and Spiceworks for threads posted in 2026 mentioning "SMTP AUTH" together with "printer," "scanner," or "OAuth" describing inability to move an embedded device off Basic Auth, and count distinct threads.
**Ідея мертва, якщо:** dead if fewer than 15 distinct threads across these forums describe this specific device problem in the past 6 months


## Розглянуто й відхилено
- **Chrome removing Manifest V2 extensions from the Web Store** — kill-test action is empty or too short (<20 chars)
- **QuickBooks Desktop discontinuation and lossy migration tooling** — kill-test action is empty or too short (<20 chars)
- **SAP ECC end of mainstream maintenance forcing rushed S/4HANA migrations** — kill-test action is empty or too short (<20 chars)
- **CA/Browser Forum shortening TLS certificate lifetimes to 47 days by 2029** — kill-test action is empty or too short (<20 chars)
- **Staggered global 2G/3G network sunsets stranding IoT devices** — kill-test action is empty or too short (<20 chars)
- **GitHub Actions runner image deprecations breaking CI pipelines** — kill-test action is empty or too short (<20 chars)
- **PayPal Website Payments Standard sunset breaking old "Buy Now"/"Add to Cart" button integrations** — kill-test action is empty or too short (<20 chars)
- **Adobe SAINT classification importer deprecation disrupting Adobe Analytics campaign tracking** — kill-test action is empty or too short (<20 chars)
- **European Accessibility Act compliance for small e-commerce/consumer sites** — kill-test action is empty or too short (<20 chars)
- **EU Cyber Resilience Act incident/vulnerability reporting for small software vendors** — kill-test action is empty or too short (<20 chars)
- **France e-invoicing mandate for micro-enterprises and SMEs** — kill-test action is empty or too short (<20 chars)
- **Poland KSeF e-invoicing mandate for VAT-registered small businesses** — kill-test action is empty or too short (<20 chars)
- **EU Deforestation Regulation (EUDR) due diligence for small importers** — kill-test action is empty or too short (<20 chars)
- **NIS2 Directive minimum cybersecurity measures for small "important entities"** — kill-test action is empty or too short (<20 chars)
- **New US state consumer-privacy laws taking effect January 2026** — kill-test action is empty or too short (<20 chars)
- **PCI DSS 4.0.1 payment-page script monitoring for small online merchants** — kill-test action is empty or too short (<20 chars)
- **EU Packaging and Packaging Waste Regulation (PPWR) EPR verification for small Amazon/marketplace sellers** — kill-test action is empty or too short (<20 chars)
- **Malaysia LHDN e-invoicing rollout to small businesses (Phase 4)** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **React Native "New Architecture" migration silently multiplies build time and bundle size** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Docker Compose lacks a "pull only if missing" policy for `docker compose pull`** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **n8n has no built-in version control for workflows** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **GitHub's own feature-request signal (reactions) gets silently discarded via issue locking/closing** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Jellyfin has no hardware-transcoder fallback/prioritization** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **GPU-accelerated scaling missing from ffmpeg-based transcode pipelines (Tdarr)** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Terraform has no ergonomic way to conditionally create resources/modules without index-based addressing** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Polars' Excel export/import loses or mis-formats per-cell types compared to pandas** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Nextcloud Desktop's sync-conflict resolution UI is confusing/unreliable** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Rust's native async trait support still doesn't cover dyn-compatible (trait object) usage** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Field-service mobile inspection reports as a narrow wedge into construction software** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Lean invoicing for freelancers without forced payment processors or SMB bloat** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Course-hosting platform fees eating into new creators' revenue before they have any** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Nonprofit donor-management CRMs seen as expensive and outdated by small nonprofits** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **AI subtitle translation breaks down on long-form video (40+ minutes)** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Agencies/freelancers waste outreach time on prospects who already have a vendor** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Generalist salon booking software doesn't fit barbershops' walk-in + appointment mix** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Solopreneurs want a single branded booking page across multiple meeting/service types, not Calendly's model** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **VS Code SFTP extension chain-abandonment (liximomo → Natizyskunk, now stalling too)** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Go decimal library (shopspring/decimal) left unmaintained after acquisition** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **VS Code Todo Tree extension abandoned, now fragmenting into competing forks** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Home Assistant Matter Hub loses its maintainer, community forks fragment** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Kiwi Browser (Android Chromium browser with extension support) discontinued** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **WordPress "Paid Memberships Pro" pulled from the official plugin directory** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Booklore self-hosted digital library vanishes overnight, forks fragment** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Kubernetes Ingress NGINX controller retirement leaves half of clusters needing migration** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **pgBackRest, PostgreSQL's dominant backup tool, briefly went unmaintained after its sole corporate sponsor was sold** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **MarkText Electron markdown editor's on-again/off-again abandonment splits its user base across forks** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Silent prompt regressions with no CI-style safety net** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **No per-feature/per-customer attribution for LLM API spend** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **MCP server debugging has almost no visibility beyond the wire protocol** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Multi-agent handoff failures are invisible to single-agent debugging tools** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Agent eval suites go stale faster than teams can maintain them** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **LLM vendor lock-in makes switching providers a multi-layer rebuild, not a config change** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **LLM red-teaming tools aren't built for agentic, multi-turn attack surfaces** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Fine-tuning dataset curation remains manual, noisy, and under-tooled** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **No good way to test LLM apps for non-deterministic output without flaky-test pain** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Teams conflate context window with memory, causing repeated agent failures** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Daycare/childcare waitlist and enrollment tracking** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Contractor permit application tracking across multiple city/county portals** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Nonprofit grant tracking and reporting-deadline management** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Church volunteer scheduling via spreadsheets, group texts, and hallway conversations** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Home inspection business scheduling and lead-to-booking workflow** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **School district substitute teacher scheduling and compliance tracking** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Small self-storage operator unit availability tracking** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Moving company lead follow-up management** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Medical practice prior authorization tracking** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Small manufacturing shop floor job tracking (whiteboard/spreadsheet)** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Cross-provider wrapper for Apple's newly opened Foundation Models framework** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Curated/trust-scored sub-registry for MCP (Model Context Protocol) servers** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Small-merchant tooling for Agentic Commerce Protocol (ACP) product feeds** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Unified integration layer across fragmented agentic-payment protocols (Visa TAP, Mastercard Agent Pay, Stripe ACP, Google UCP)** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Migration/compatibility tooling for Reddit's Developer Platform transition** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Tooling for community banks/credit unions adopting FedNow's new Network Intelligence API** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Official Python SDK gap for Bluesky/AT Protocol's new "lex" client generation system** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Cross-bank open-banking API conformance/testing tool amid CFPB Section 1033 uncertainty** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Reconciliation/compliance tooling for banks adopting Circle's new stablecoin settlement API** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Manual per-client photo gallery delivery is a "giant time suck"** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Gym/studio owners feel trapped by opaque, PE-owned management software pricing** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **No itemized recurring-expense entries in Zoho Books forces a risky journal-entry workaround** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Small professional-services firms burn hours chasing client documents across scattered channels** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Churches still hand-schedule volunteers in spreadsheets and email reminders** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Newsletter writers track sponsorship deals and rates by hand in spreadsheets** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Etsy's own shipment-tracking data misreports delivery status, triggering wrongful refund claims** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **QuickBooks Online users manually untangle duplicate bank-feed transactions during reconciliation** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Postman killed free team collaboration, pushing small teams to $19/user/mo** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Docker Desktop subscription price hikes (up to 80%) pushing teams to Podman/Colima** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **monday.com's 3-seat minimum plus fixed seat bundles overcharge tiny teams** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **the "SSO tax" — vendors charging 2-4x more to unlock basic single sign-on** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **1Password Business's 3-seat minimum and steep "pricing cliff" above 10 users** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Small businesses paying full Salesforce price for ~20% of its features, plus annual renewal hikes** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Atlassian ending Data Center licenses (Mar 2026) forces a forced cloud move plus a 2026 price hike** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Miro's Free/Starter plans silently convert invited guests into paid "members," triggering surprise annual charges** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **Calendly gates SSO and Salesforce integration behind paid tiers, adding a per-user "security tax"** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
- **DocuSign's 10-20% renewal price hikes and add-on fees push mid-market customers toward alternatives** — filter produced no verdict for this candidate — treated as rejected, not silently dropped
